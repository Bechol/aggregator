<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="aggregator"/>

    <contextName>${appName}</contextName>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="!local">
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http>
                <url>http://192.168.68.105:3100/loki/api/v1/push</url>
                <requestTimeoutMs>30000</requestTimeoutMs>
            </http>
            <labels>
                app=${appName}
                host=${HOSTNAME}
                level=%level
            </labels>
            <message>
                <pattern>${HOSTNAME} %level [%thread] %logger{20} | %msg %ex</pattern>
            </message>
            <verbose>true</verbose>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="LOKI" />
        </root>
    </springProfile>

    <springProfile name="local">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

</configuration>